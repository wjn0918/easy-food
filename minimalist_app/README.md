# 极简菜谱日历程序

> 该目录是基于 `docs/` 菜谱内容重新实现的最小可用程序，`docs/` 原文件完全不修改。

## 功能

1. 记录每天早餐/午餐/晚餐吃了哪道菜（可留空）。
2. 菜谱日历：按日期展示三餐记录。
3. 按菜品统计每周、每月被吃的次数。

## 运行

```bash
python3 minimalist_app/app.py
```

数据会保存到：

- `minimalist_app/meal_records.json`
